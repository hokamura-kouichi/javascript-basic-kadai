function squareNumber(number) {
    // 引数として受け取った数値を二乗します
    let result = number * number;
    // 計算結果を返します
    return result;
}

// 例として引数に10を渡して関数を実行します
let result = squareNumber(10);

// 計算結果をコンソールに出力します
console.log(result);