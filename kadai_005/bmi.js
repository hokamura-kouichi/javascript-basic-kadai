// 体重（kg）を変数に代入します
let weight = 68; // ここに実際の体重を入れてください

// 身長（m）を変数に代入します
let height = 1.7; // ここに実際の身長を入れてください（メートル単位です）

// BMIを計算します
let bmi = weight / (height * height);

// 結果を出力します
console.log("あなたのBMIは " + bmi + " です。");